<div class="container">
  <div class="wrapper col-md-8 col-md-offset-2 col-sm-10">
    <h2>お知らせ</h2>
    <ul>
      <% @notifications.each do |notification| %>
      <hr>
          <span><%= read_unread(notification.read) %></span>
          <p>
            <%= link_to notification.comment.user.try(:name), user_path(notification.comment.user.id) %>さんが
            あなたのブログ（<%= link_to notification.comment.blog.title, blog_path(notification.comment.blog.id) %>）にコメントしました。
          </p>
          <p><%= posted_time(notification.created_at) %>前（<%= notification.created_at.strftime("%Y/%m/%d %p %l:%M") %>）</p>
      <% end %>
    </ul>
  </div>
</div>