<div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
  <div>
    <h1>新規依頼</h1>
    <%= link_to "タスク一覧", user_tasks_path(current_user.id), class: 'btn btn-default' %> | 
    <%= link_to '依頼されている一覧', inbox_user_submit_requests_path, class: 'btn btn-default' %> | 
    <%= link_to '依頼している一覧', user_submit_requests_path, class: 'btn btn-default' %>
    <div>
      </br>
      <% if @users.blank? %>
        依頼するユーザーがいません。
      <% elsif current_user.tasks.present? %>
        <%= render 'form' %>
      <% else %>
        依頼できるものがありません。
      <% end %>
    </div>
  </div>
</div>